{
    "name": "meme-marketplace-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix meme-backend\" \"npm run dev --prefix meme\"",
        "install:all": "npm install --prefix meme-backend && npm install --prefix meme",
        "start": "npm run dev"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}